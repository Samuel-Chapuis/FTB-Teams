package dev.ftb.mods.ftbteams.world.block;

import net.minecraft.world.level.block.state.BlockBehaviour;

/** First enclosure block; its visual and interaction behaviour is temporary. */
public class SmallBedBlock extends Enclosure {
	public SmallBedBlock(BlockBehaviour.Properties properties) {
		super(properties);
	}
}
